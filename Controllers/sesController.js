var Config = require('../Core/Config') 
   ,Helper = require(Config.dirCore+'Helper');
   
// описываем наш контроллер
Config.App.ns('sesController').method({
    set : function(param){
        
    },
});